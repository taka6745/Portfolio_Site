import React from "react";
import "./Layout.css";
// Get from github, save all as physical json, update on /update

const ProjectBlock = ({ data }) => {
  return (
    <>
      <h1>THIS IS {data}</h1>
    </>
  );
};

export default ProjectBlock;
